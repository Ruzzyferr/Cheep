# Cheep Scraper - Gerekli Paketler

# HTTP İstekleri (Migros API için)
requests==2.34.2

# Web Scraping (CarrefourSA için)
# Not: Python 3.12 için minimum sürümler gevşetildi (eski pin'lerin cp312 wheel'i yok).
playwright>=1.62.0
beautifulsoup4==4.15.0

# Veri İşleme
lxml>=6.1.2  # BeautifulSoup için parser (py3.12 wheel)

# Scalable LLM Matcher için
numpy>=2.5.2  # Cosine similarity hesaplamaları için (py3.12 wheel)
python-dotenv==1.2.3  # .env dosyası desteği için

# Kafka producer (INGEST_MODE=kafka) — raw.products.v1'e Avro üretimi
confluent-kafka==2.15.0
fastavro==1.12.2

# Test
pytest==9.1.1

# Macaristan (Arfigyelo) gunluk dokumu XLSX olarak yayinlaniyor.
openpyxl>=3.1
